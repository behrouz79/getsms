<?xml version="1.0" encoding="utf-8"?>
<!-- StatusStrip row. Root background is set programmatically:
     strip_success (green rail) or strip_error (red rail).
     Padding: 13dp start (3dp rail + 10dp inner), 12dp end, 10dp top/bottom. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:layout_marginBottom="8dp"
    android:background="@drawable/strip_success"
    android:paddingStart="13dp"
    android:paddingEnd="12dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <!-- Row 1: action badge (amber) + destination (monospace) | timestamp -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Action type: WEBHOOK / TELEGRAM / SMS -->
        <TextView
            android:id="@+id/tvActionType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="6dp"
            style="@style/Text.Badge"
            android:textColor="@color/primary" />

        <!-- Destination (monospace, ellipsized) -->
        <TextView
            android:id="@+id/tvDestination"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            style="@style/Text.Mono"
            android:textColor="@color/text_primary"
            android:maxLines="1"
            android:ellipsize="end" />

        <!-- Timestamp -->
        <TextView
            android:id="@+id/tvTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            style="@style/Text.Caption"
            android:textColor="@color/text_muted" />

    </LinearLayout>

    <!-- Row 2: meta "142 ms · 200 OK" + optional badges -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginTop="4dp">

        <TextView
            android:id="@+id/tvDuration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            style="@style/Text.Caption"
            android:textColor="@color/text_secondary" />

        <!-- retry 2/3 -->
        <TextView
            android:id="@+id/tvRetryBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:paddingStart="7dp"
            android:paddingEnd="7dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            style="@style/Text.Badge"
            android:textColor="@color/text_secondary"
            android:background="@drawable/bg_badge_neutral"
            android:visibility="gone" />

        <!-- backup ✓ -->
        <TextView
            android:id="@+id/tvBackupBadge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="6dp"
            android:paddingStart="7dp"
            android:paddingEnd="7dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:text="backup ✓"
            style="@style/Text.Badge"
            android:textColor="@color/info"
            android:background="@drawable/bg_badge_info"
            android:visibility="gone" />

    </LinearLayout>

    <!-- Row 3: rule / from info -->
    <TextView
        android:id="@+id/tvFrom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        style="@style/Text.Caption"
        android:textColor="@color/text_muted"
        android:maxLines="1"
        android:ellipsize="end"
        android:visibility="gone" />

</LinearLayout>
